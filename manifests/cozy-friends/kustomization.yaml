apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - backup-hooks-configmap.yaml
  - pvc-pack.yaml
  - service-headless.yaml
  - service-gameplay.yaml
  - service-backup-metrics.yaml
  - statefulset.yaml
  - servicemonitor.yaml
  - networkpolicy.yaml
# cozy-friends.secret.yaml is intentionally excluded. Decrypt and apply it
# locally with SOPS before syncing this application.
