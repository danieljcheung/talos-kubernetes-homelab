apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apply-agent-data
  namespace: apply-agent
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
