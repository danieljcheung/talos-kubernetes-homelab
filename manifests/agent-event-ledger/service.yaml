apiVersion: v1
kind: Service
metadata:
  name: agent-event-ledger
  namespace: agent-event-ledger
  labels:
    app.kubernetes.io/name: agent-event-ledger
spec:
  selector:
    app.kubernetes.io/name: agent-event-ledger
  ports:
    - name: http
      port: 8080
      targetPort: http
