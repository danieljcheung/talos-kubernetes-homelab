apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: company-brain-db-daily
  namespace: company-brain
spec:
  schedule: "0 0 7 * * *"
  backupOwnerReference: self
  cluster:
    name: company-brain-db
