apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: company-brain

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - migration-job.yaml
  - tailscale-ingress.yaml
