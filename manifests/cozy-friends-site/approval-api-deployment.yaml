apiVersion: apps/v1
kind: Deployment
metadata:
  name: cozy-friends-approval-api
  namespace: cozy-friends-site
  labels:
    app.kubernetes.io/name: cozy-friends-approval-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cozy-friends
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: cozy-friends-approval-api
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cozy-friends-approval-api
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: cozy-friends
    spec:
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: ghcr-danieljcheung
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: approval-api
          image: ghcr.io/danieljcheung/cozy-friends-approval-api@sha256:5265bb70fe72e81fc4bb09eb5bb91ad7c2e59fd28e7559aba1e4599c2cf44cc8
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: PORT
              value: "8080"
            - name: PYTHONDONTWRITEBYTECODE
              value: "1"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: cozy-friends-approval-db-app
                  key: uri
            - name: APPROVAL_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cozy-friends-approval-secrets
                  key: admin-token
            - name: APPROVAL_SYNC_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cozy-friends-approval-secrets
                  key: sync-token
            - name: TURNSTILE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: cozy-friends-approval-secrets
                  key: turnstile-secret-key
            - name: TURNSTILE_EXPECTED_HOSTNAME
              value: cozy.popinvites.com
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz/readiness
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
